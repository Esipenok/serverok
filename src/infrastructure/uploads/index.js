// Uploads infrastructure exports
// This module handles file uploads and storage
// Currently contains temp and users directories

module.exports = {
  // Upload directories are handled by the file system
  // This is mainly for organization purposes
}; 